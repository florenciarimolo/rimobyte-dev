---
import { LANG_PREFIXES } from '@/i18n/index';

// Redirect to Spanish version
const { city } = Astro.params;
const normalizedCity = city?.toLowerCase();
return Astro.redirect(`${LANG_PREFIXES.SPANISH}/migrar-web-agencia-freelance/${normalizedCity}`, 301);
---
