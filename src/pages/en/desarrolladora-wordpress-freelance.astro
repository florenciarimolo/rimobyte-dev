---
import MainLayout from '@/layouts/MainLayout.astro';
import WordPressLandingPage from '@/components/WordPressLandingPage';
import WordPressStructuredData from '@/components/wordpress-landing/WordPressStructuredData.astro';
import { LANG } from '@/i18n/index';
import { SITE_URL } from '@/constants';

const locale = LANG.ENGLISH;

const currentUrl = `${SITE_URL}${Astro.url.pathname}`;
const title = 'WordPress Freelance Developer for Professional Businesses | RimoByte';
const description = 'WordPress freelance developer specialized in professional websites, e-commerce and maintenance. Direct work without intermediaries, optimized for performance and SEO.';

// Get reCAPTCHA site key from environment variable
const recaptchaSiteKey = import.meta.env.RECAPTCHA_SITE_KEY || '6LeIxAcTAAAAAJcZVRqyHh71UMIEGNQ_MXjiZKhI';
---

<MainLayout 
  title={title}
  description={description}
  locale={locale}
>
  <WordPressStructuredData 
    url={currentUrl}
    city={null}
  />
  <WordPressLandingPage client:only="react" recaptchaSiteKey={recaptchaSiteKey} initialLocale={locale} />
</MainLayout>
